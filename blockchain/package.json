{
  "name": "bookchain",
  "version": "0.1.0",
  "description": "bookchain",
  "main": "index.js",
  "scripts": {
    "truffle": "truffle",
    "test": "rm -rf build/ && truffle compile/ && truffle test",
    "deploy:testrpc": "rm -rf build/ && truffle migrate --network testrpc",
    "deploy:prod": "rm -rf build/ && truffle migrate --network prod",
    "build": "truffle compile"
  },
  "author": "hht",
  "private": true,
  "dependencies": {
    "debug": "^3.0.1",
    "ethereumjs-testrpc": "~6.0.3",
    "mocha": "^4.1.0",
    "n": "^4.1.0",
    "truffle-privatekey-provider": "^1.1.0",
    "vue": "^2.6.10",
    "web3": "^v1.0.0-beta.55",
    "zeppelin-solidity": "~1.7.0"
  }
}
